---
title: A feature tour
type: content
---

:spacer:

## A feature tour

This deck steps through every component the framework ships, in roughly the order you'd reach for them.

- Headings, paragraphs, lists — the markdown core
- +++ Quotes and code blocks
- +++ Images and box-and-arrow diagrams
- +++ 3D scenes and SVG data viz
- +++ All composed in one deck

+++ !muted Use the arrow keys to step through. `Escape` opens the command palette.

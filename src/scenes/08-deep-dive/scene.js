import { createSectionSlide } from '../../section-slides/index.js';
import { colors } from '../../shared/colors.js';

export const deepDiveScene = createSectionSlide('Deep Dive', {
  subtitle: 'under the hood',
  accent: colors.beam,
});
